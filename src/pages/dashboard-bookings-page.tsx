import { BookingsPage } from '@/features/bookings/bookings-page'

export function DashboardBookingsPage() {
  return (
    <BookingsPage
      role="professional"
      title="Bookings"
      description="Review requests, manage upcoming sessions, and track completed work."
    />
  )
}
